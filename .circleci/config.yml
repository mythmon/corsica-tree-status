version: 2

jobs:
  build_test:
    docker:
      - image: circleci/node:9@sha256:52a61dadb97719b4c673ad97eddb11c95dbf601872c0938063f9c271ca26a354

    steps:
      - checkout

      - restore_cache:
          keys:
            - yarn-cache-{{ checksum "yarn.lock" }}
            - yarn-cache-

      - run:
          name: Yarn Install
          command: yarn install --cache-folder ~/.cache/yarn

      - save_cache:
          key: yarn-cache-{{ checksum "yarn.lock" }}
          paths:
            - ~/.cache/yarn

      - run:
          name: Build
          command: yarn build

      - run:
          name: Test
          command: yarn test

workflows:
  version: 2
  pr:
    jobs:
      - build_test
