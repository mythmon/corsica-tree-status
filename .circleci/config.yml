version: 2

jobs:
  build_test:
    docker:
      - image: circleci/node:9@sha256:1827e439da2b28b55948418db6037a926b1a5a407cb1352b0692203e8bab79c9

    steps:
      - checkout

      - restore_cache:
          keys:
            - yarn-cache-{{ checksum "yarn.lock" }}
            - yarn-cache-

      - run:
          name: Yarn Install
          command: yarn install --cache-folder ~/.cache/yarn

      - save_cache:
          key: yarn-cache-{{ checksum "yarn.lock" }}
          paths:
            - ~/.cache/yarn

      - run:
          name: Build
          command: yarn build

      - run:
          name: Test
          command: yarn test

workflows:
  version: 2
  pr:
    jobs:
      - build_test
