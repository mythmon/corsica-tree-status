{
  "name": "corsica-tree-status",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "classnames": "2.2.6",
    "react": "16.7.0",
    "react-dom": "16.7.0",
    "react-scripts": "1.1.5"
  },
  "homepage": "https://corsica-tree-status.now.sh",
  "scripts": {
    "postinstall": "yarn flow-typed install -s",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "flow": "flow",
    "now-build": "yarn build"
  },
  "devDependencies": {
    "flow-bin": "0.94.0",
    "flow-typed": "2.5.1",
    "nsp": "3.2.1"
  }
}
